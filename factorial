#include <stdio.h>

int main() {
    int N = 4;
    int factorial = 1;

    for (int i = 1; i <= N; i++) {
        factorial *= i;
    }

    printf("Factorial of %d is: %d\n", N, factorial);

    return 0;
}
